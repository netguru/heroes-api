#import Post from '../Post/post-schema.graphql'

type User {
	id: ID!
	email: String
	name: String!
	posts: [Post!]!
}

type Mutation {
	createNewUser(email: String!, name: String!): User!
}

type Query {
	getAllUsers: [User!]!
}
